-- ค่านายหน้า (broker/agent commission) ต่อขา — มักเกิดกับขากลับ (backhaul) ที่ใช้
-- นายหน้าหาสินค้าเที่ยวกลับ. เป็นต้นทุนของรอบ หักจากกำไรเหมือน "ค่าใช้จ่ายอื่นๆ".
--
-- nullable โดยตั้งใจ: NULL = ยังไม่ได้ระบุ (หน้าปิดงานจะเตือนให้กรอก), 0 = ยืนยันว่า
-- ไม่มีค่านายหน้า. ขาเก่าทั้งหมดเป็น NULL ⇒ ไม่กระทบยอดเดิม (แอปนับ NULL เป็น 0).
ALTER TABLE dispatch_legs ADD COLUMN IF NOT EXISTS commission numeric;
