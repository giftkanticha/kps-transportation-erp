# <Workflow Name>

> One-line summary of what this workflow accomplishes.

## Objective
What "done" looks like. Be concrete about the final deliverable.

## Inputs
| Input | Required | Source / How to get it |
|-------|----------|------------------------|
| e.g. date range | yes | user-provided |
| e.g. API key | yes | `.env` → `SOME_API_KEY` |

## Tools
Run in this order:
1. `tools/<script>.py` — what it does, key args
2. `tools/<script>.py` — what it does, key args

## Output
- Where the deliverable lands (e.g. a Google Sheet URL, a Supabase table)
- Format / schema notes

## Edge cases & lessons learned
- Known failure modes and how to recover
- Rate limits, timing quirks, gotchas discovered while running this
